# 代码审查记录：models / parser / rules（DeepSeek）

- 日期：2026-09-22
- 审查人：DeepSeek（联合执行者，协作模式 v2.0 硬门）
- 结论：**不可提交**（原版）；修复后全部阻塞项已处理，17 项测试通过。
- 原文归档：DeepSeek 全文见本目录（审查输出落盘时保存要点，原文已按规范摘要记录）。

## 阻塞项处理（Blocking，全部完成）

| 编号 | 问题 | 处理 |
|---|---|---|
| H1 | r3 票号解析无保护，脏票击穿"不中断整批" | `_tail()` 保护 + 不可解析票记低危提示 |
| H2 | 空票号/零金额被 R1 判"确定"级重复（触碰红线） | 不完整票不判重，降级低危/疑似提示 |
| H3 | 金额采到明细行（setdefault 语义错误） | 合计类标签取"最后出现"+收敛歧义别名 |
| H4 | parse_xml 抛异常中断批次 | detect_type 前置+类型化 ValueError；文件级隔离约定在 main.py 层 |
| H5 | detect_type 对 BOM/无 prolog 合法 XML 误判 | 剥 BOM + 直接根节点判定（含测试） |
| H6 | raw_fields/evidence 敏感数据未脱敏 | raw_fields 白名单 + 税号脱敏（含测试） |
| H7 | RulesConfig 默认示例公司，忘配置全票误报 | company 无默认；未配置时 R2 降级"未执行"提示（含测试） |
| H8 | 单规则异常拖垮整批 | 逐规则 try/except 隔离，异常降级批次级 Finding（含测试） |

## 中危处理

- M1 seller_taxid 补入模型，R3/R6 按 (名称,税号) 分组 ✓
- M2 空 supplier/date 不参与分组，记低危提示 ✓
- M3 日期多格式（含 2024-1-5）✓
- M4 金额剥千分位/货币符号 ✓
- M6 R4 口径改用价税合计 ✓
- M8 排序加稳定 tie-breaker ✓
- M10 Finding 增 ruleset_version 字段 ✓
- M13 R7 增"开票晚于今天"分支 ✓
- M14 R1 增 source_hash 整文件重复检测 ✓
- M15 名称/税号缺失记疑似明示 ✓
- M9 文件大小上限 ✓（10MB）；元素数上限/子节点取值留 P2
- M11 batch_id 一致性校验：P1 由 main.py 单批保证（文档注明）
- M12 红冲/负数/作废状态：模型缺 status 字段，留 P2（已记录）
- M5 全半角归一：名称比较已去空格；全角转半角留后续

## 低危处理

- L3 source_hash [:16]→[:32] ✓；L5 常量统一 ✓；L7 输出层转义在 report.py 阶段落实；其余记录待办。

## 测试覆盖

- parser 6 项（含 BOM/无 prolog/非XML拒绝/超大文件/税号脱敏）
- rules 11 项（含红线保护 x3 / 脏票号隔离 / 排序 / 跨票架构）
- 结论措辞：公开规则+合成/构造用例通过，真实分布未验证；输出均为辅助提示。
